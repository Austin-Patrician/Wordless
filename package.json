{
  "name": "wordless",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "npm run check --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/capabilities/*",
    "packages/profiles/*"
  ]
}
