# @wordless/protocol

Serializable commands, responses, event envelopes, error contracts, and runtime validation schemas shared across Electron processes.

This package may depend on `@wordless/domain`. It must not expose Electron objects, class instances, provider SDK types, or raw `@wordless/agent` events.
