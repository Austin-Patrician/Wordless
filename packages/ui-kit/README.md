# @wordless/ui-kit

Reusable React presentation primitives and the session event reducer shared by all desktop profile views.

It may consume `@wordless/domain` and `@wordless/protocol`, but must not access Electron IPC directly or import runtime, persistence, Node, AI, or Agent packages.
