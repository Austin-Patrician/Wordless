# @wordless/profile-sdk

Contracts for defining an Agent Profile and composing reusable capabilities.

A profile declares prompts, tools, model requirements, permissions, artifact kinds, and lifecycle hooks. It must not own persistence, IPC, credential storage, or global runtime state.
