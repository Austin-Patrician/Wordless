# @wordless/domain

Pure Wordless domain definitions: projects, sessions, runs, artifacts, permissions, credential references, and repository ports.

This package must remain independent of Electron, React, persistence implementations, profiles, and the AI runtime.
