# @wordless/platform-node

Node.js adapters for `ExecutionEnv`, filesystem access, process execution, path canonicalization, temporary files, and cancellation.

Electron-specific APIs remain in `apps/desktop/src/main/adapters`; this package must not import Electron.
