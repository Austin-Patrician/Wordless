# Capabilities

Reusable Agent tool implementations and context contributors. Capabilities are scenario-neutral and receive platform services, approval services, workspace scope, and artifact services through explicit context.

Capabilities must not import concrete profiles or Electron.
